class Game {
  /**
   * Show all games parsed from log
   */
  list (parser){
    return (request, reply) => {
    }
  }

  /**
   * Expose one game and print your date
   */
  show (parser) {
    return (request, reply) => {
    }
  }
}

const game = new Game();
module.exports = game;
